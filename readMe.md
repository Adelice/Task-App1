1. create the folder structure/name
2. navigate to the folder 
3. Create the virtual: python -m venv venv
4. activate the virtual: source venv/bin/activate
5. Install Flask: pip install Flask 
6. create a file named app.py 
 